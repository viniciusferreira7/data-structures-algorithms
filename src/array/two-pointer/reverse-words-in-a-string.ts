
const input = "the sky is blue"

/**
 * Reverses the order of words in a given string, removing extra spaces.
 *
 * Given a string `s`, this function returns a new string with the words in reverse order.
 * Leading and trailing spaces are removed, and multiple spaces between words are reduced to a single space.
 *
 * @param str - The input string containing words separated by spaces.
 * @returns A new string with the words in reverse order and cleaned spacing.
 *
 * @example
 * reverseWordsInAString("the sky is blue");
 * // Returns: "blue is sky the"
 *
 * @example
 * reverseWordsInAString("  hello world  ");
 * // Returns: "world hello"
 *
 * @example
 * reverseWordsInAString("a good   example");
 * // Returns: "example good a"
 */
export function reverseWordsInAString(str: string): string {
  // ...
}
